pipeline {
    agent any
    stages {
        stage('Check Git') {
            steps {
                sh 'git remote -v'
                sh 'git branch -a'
            }
        }
        stage('Build') {
            steps {
                sh 'npm install'
                sh 'npm start'
            }
        }
    }
}
